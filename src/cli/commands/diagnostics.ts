import { defineCommand } from "citty";
import { applyDiagnosticsExportFilter, buildSessionDigest, buildSessionTimeline, listDiagnosticsRuns, managedDiagnosticsBundle, managedDiagnosticsExport, readDiagnosticsRunView } from "#engine/diagnose/export.js";
import { sharedArgs } from "#cli/args.js";
import { firstPos, num, print, session, str, withCliError, type CliArgs } from "./_helpers.js";

const exportCmd = defineCommand({ meta: { name: "export", description: "Export diagnostics records" }, args: { ...sharedArgs, section: { type: "string", description: "Section", valueHint: "section" }, limit: { type: "string", description: "Limit", valueHint: "n" }, since: { type: "string", description: "Since ISO", valueHint: "iso" }, text: { type: "string", description: "Text filter", valueHint: "text" }, fields: { type: "string", description: "Fields projection", valueHint: "fields" } }, async run({ args }) { const a = args as CliArgs; try { const sn = session(a); const exported = await managedDiagnosticsExport({ sessionName: sn }); print("diagnostics export", applyDiagnosticsExportFilter(exported, { section: str(a.section) as never, limit: num(a.limit), since: str(a.since), text: str(a.text), fields: str(a.fields) }) as Parameters<typeof print>[1], a); } catch (e) { withCliError("diagnostics export", a, e); } } });
const bundle = defineCommand({ meta: { name: "bundle", description: "Build diagnostics bundle" }, args: { ...sharedArgs, limit: { type: "string", description: "Limit", default: "20", valueHint: "n" } }, async run({ args }) { const a = args as CliArgs; try { const sn = session(a); const exported = await managedDiagnosticsExport({ sessionName: sn }); print("diagnostics bundle", await managedDiagnosticsBundle({ sessionName: sn, limit: num(a.limit), exported }) as Parameters<typeof print>[1], a); } catch (e) { withCliError("diagnostics bundle", a, e); } } });
const runs = defineCommand({ meta: { name: "runs", description: "List recorded runs" }, args: { ...sharedArgs, limit: { type: "string", description: "Limit", valueHint: "n" }, since: { type: "string", description: "Since ISO", valueHint: "iso" } }, async run({ args }) { const a = args as CliArgs; try { const sn = str(a.session); const items = await listDiagnosticsRuns({ sessionName: sn, limit: num(a.limit), since: str(a.since) }); print("diagnostics runs", { data: { count: items.length, runs: items } }, a); } catch (e) { withCliError("diagnostics runs", a, e); } } });
const digest = defineCommand({ meta: { name: "digest", description: "Summarize diagnostics" }, args: { ...sharedArgs, run: { type: "string", description: "Run id", valueHint: "id" }, limit: { type: "string", description: "Limit", default: "5", valueHint: "n" } }, async run({ args }) { const a = args as CliArgs; try { if (str(a.run)) { const view = await readDiagnosticsRunView({ runId: str(a.run) as string, limit: num(a.limit) }); print("diagnostics digest", { data: view }, a); return; } const sn = session(a); print("diagnostics digest", buildSessionDigest(await managedDiagnosticsExport({ sessionName: sn }), num(a.limit, 5) as number) as Parameters<typeof print>[1], a); } catch (e) { withCliError("diagnostics digest", a, e); } } });
const show = defineCommand({ meta: { name: "show", description: "Show run events" }, args: { ...sharedArgs, command: { type: "string", description: "Command filter", valueHint: "name" }, text: { type: "string", description: "Text filter", valueHint: "text" }, limit: { type: "string", description: "Limit", valueHint: "n" }, since: { type: "string", description: "Since ISO", valueHint: "iso" }, fields: { type: "string", description: "Fields", valueHint: "fields" } }, async run({ args }) { const a = args as CliArgs; try { print("diagnostics show", { data: await readDiagnosticsRunView({ runId: firstPos(a) as string, command: str(a.command), text: str(a.text), limit: num(a.limit), since: str(a.since), fields: str(a.fields) }) }, a); } catch (e) { withCliError("diagnostics show", a, e); } } });
const grep = defineCommand({ ...show, meta: { name: "grep", description: "Search run events" } });
const timeline = defineCommand({ meta: { name: "timeline", description: "Build session timeline" }, args: { ...sharedArgs, limit: { type: "string", description: "Limit", default: "50", valueHint: "n" }, since: { type: "string", description: "Since ISO", valueHint: "iso" } }, async run({ args }) { const a = args as CliArgs; try { const sn = session(a); const exported = await managedDiagnosticsExport({ sessionName: sn }); print("diagnostics timeline", { session: exported.session as Record<string, unknown>, page: exported.page as Record<string, unknown>, data: await buildSessionTimeline({ sessionName: sn, limit: num(a.limit), since: str(a.since), exported }) }, a); } catch (e) { withCliError("diagnostics timeline", a, e); } } });

export default defineCommand({ meta: { name: "diagnostics", description: "Diagnostics export, runs, digest and bundle" }, subCommands: { export: exportCmd, bundle, runs, digest, show, grep, timeline } });
